This directory needs to be populated with file containing base-64 encoded of images stored in `img` folder.

Kindly run `npx tsx imgToBase64.ts` from the root directory of this repo.

Visual Studio Code users can facilitate configured task "Resources: png images to base 64" under the "Terminal" -> "Run task…"